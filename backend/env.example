# Variables de entorno del backend — RPA Académico PUCP
# Copiar este archivo como ".env" y completar con los valores reales.
# El archivo ".env" NUNCA debe subirse al repositorio (ver .gitignore).

# Base de datos MySQL
DB_USER=root
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=3306
DB_NAME=rpa_academico_pucp
